/**
 * Homepage section DOM ids — single source for in-page anchors and deep links
 * (e.g. live chat, hash navigation).
 */
export const HOME_WHY_SSP_SECTION_ID = "home-why-ssp" as const;
export const HOME_WHY_SSP_HEADING_ID = "home-why-ssp-heading" as const;
