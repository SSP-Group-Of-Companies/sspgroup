// src/app/(site)/services/_constants.ts
import { HEADER_HEIGHT_PX } from "../components/layout/header/constants";

/** Sticky subnav sits directly below header */
export const SERVICE_SUBNAV_TOP_PX = HEADER_HEIGHT_PX;

/** Breathing room below sticky UI when jumping to anchors */
export const SERVICE_ANCHOR_GAP_PX = 16;
