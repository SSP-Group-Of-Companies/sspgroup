import { HEADER_HEIGHT_PX } from "@/app/(site)/components/layout/header/constants";

/** Sticky subnav sits directly below header */
export const FAQ_SUBNAV_TOP_PX = HEADER_HEIGHT_PX;

/** Breathing room below sticky UI when jumping to anchors */
export const FAQ_ANCHOR_GAP_PX = 16;
